generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model BobotPreset {
  id        Int      @id @default(autoincrement())
  nama      String
  bobot     String
  createdAt DateTime @default(now())
}
